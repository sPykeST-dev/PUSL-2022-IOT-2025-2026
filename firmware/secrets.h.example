// Copy this file to secrets.h and fill in your values.
// secrets.h is excluded from version control (.gitignore).
// NEVER commit secrets.h to the repository.

#define WIFI_SSID     "your-network-name"
#define WIFI_PASSWORD "your-network-password"
#define SERVER_URL    "http://192.168.x.x:8080"   // your laptop's IP on the shared network
#define LOCKER_ID     "LOCKER-01"                  // must match id in the lockers table
